spring:
  application:
    name: TurboMechanics
  
  web:
    resources:
      add-mappings: false

  mvc:
    static-path-pattern: /static/**

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/Turbo_Mechanics}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:santi0103}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate: 
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: TurboMechanicsTaller@gmail.com
    password: "vvjl okct lprl bomr"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: ${PORT:9090}

security:
  jwt:
    secret-key: ${JWT_SECRET_KEY:QjaJfdfFv7krX863yrVAS87tfaz56rUWcrUzS0ZbqFU=}
    token-expiration: ${JWT_TOKEN_EXPIRATION:600000}

whatsapp:
  service:
    url: ${WHATSAPP_SERVICE_URL:http://localhost:3001}
    token: ${WHATSAPP_SERVICE_TOKEN:turbo_mechanics_2026}
  session:
    default: ${WHATSAPP_DEFAULT_SESSION:admin}
    
app:
  frontend:
    url: ${APP_FRONTEND_URL:http://localhost:4200}
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:4200}

mercadopago:
  access-token: ${MERCADOPAGO_ACCESS_TOKEN:APP_USR-7726278336164374-060415-e160bbde422ec8723d23c5f42247a7b0-3450458282}
  public-key: ${MERCADOPAGO_PUBLIC_KEY:APP_USR-3fc637bb-f3a6-47bd-8557-afceab72f160}
  notification-url: ${MERCADOPAGO_NOTIFICATION_URL:https://slug-scrooge-enquirer.ngrok-free.dev/payments/webhook}

gemini:
  api:
    key: ${GEMINI_API_KEY:}